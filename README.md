- Disk Manager

**⚠️ ВАЖНО: Все операции выполняются ТОЛЬКО в изолированной виртуальной машине! Это обеспечивает полную безопасность для вашей основной системы!**

---

## 📋 Описание проекта

**Disk Manager** — это веб-приложение на основе **FastAPI**, разработанное для управления дисками в виртуальной машине на базе Ubuntu/Debian. Приложение позволяет:

- Просматривать список дисков и их разделов с информацией об имени, размере и точке монтирования.
- Выполнять операции монтирования, размонтирования и форматирования разделов через удобный веб-интерфейс.

Приложение соответствует техническому заданию, предоставляя функциональность для управления дисками и отображения их в виде таблицы с кнопками для каждой операции.

---

## 🎯 Соответствие техническому заданию

Приложение реализует все требования ТЗ:

1. **Функциональность**:
   - **Монтирование диска**: Эндпоинт `/mount` позволяет монтировать раздел в указанную точку монтирования.
   - **Размонтирование диска**: Эндпоинт `/unmount` размонтирует раздел по указанной точке монтирования.
   - **Форматирование диска**: Эндпоинт `/format` форматирует раздел в выбранную файловую систему (`ext4`, `vfat`, `ntfs`), с защитой системных разделов.
2. **Интерфейс**:
   - Диски и их разделы отображаются в структурированном виде (не таблица, но список с аналогичной информацией).
   - Для каждого раздела отображаются:
     - Имя раздела (например, `/dev/sda1`).
     - Размер раздела (например, `14.5G`).
     - Точка монтирования (например, `/media/w1zard/SANE4KA` или пустая строка).
     - Кнопки: **Монтировать**, **Размонтировать**, **Форматировать** (с выбором файловой системы).
3. **Безопасность**:
   - Приложение тестировалось в изолированной виртуальной машине (Ubuntu 24.04).
   - Защита от форматирования системных разделов (например, `/` или `/boot/efi`).

---

## 📷 Скриншот интерфейса

![Интерфейс Disk Manager](screenshot.png)*Описание скриншота*: Интерфейс показывает два диска: `/dev/sda` (флешка) и `/dev/nvme0n1` (основной диск). Для каждого раздела отображаются имя, размер, точка монтирования и три кнопки:

- **Монтировать**: Поле для ввода точки монтирования и кнопка для монтирования.
- **Размонтировать**: Кнопка активна, если раздел смонтирован, иначе отключена.
- **Форматировать**: Выпадающий список для выбора файловой системы (`ext4`, `vfat`, `ntfs`) и кнопка с подтверждением действия.

*Примечание*: Замените `screenshot.png` на реальный скриншот интерфейса, сделанный в вашей виртуальной машине.

---

## 🛠 Установка и запуск

### Требования

- Python 3.8+
- Ubuntu/Debian (в виртуальной машине)
- Утилиты: `lsblk`, `mount`, `umount`, `mkfs` (`dosfstools` и `ntfs-3g` для поддержки `vfat` и `ntfs`)
- FastAPI и зависимости

### Установка

1. **Клонируйте репозиторий**:

   ```bash
   git clone https://github.com/your-username/disk-manager.git
   cd disk-manager
   ```

2. **Создайте виртуальное окружение**:

   ```bash
   python3 -m venv venv
   source venv/bin/activate
   ```

3. **Установите зависимости**:

   ```bash
   pip install fastapi uvicorn jinja2
   ```

4. **Установите системные утилиты**:

   ```bash
   sudo apt update
   sudo apt install dosfstools ntfs-3g
   ```

5. **Настройте права** `sudo`:

   - Разрешите выполнение `mount`, `umount`, и `mkfs` без пароля:

     ```bash
     sudo visudo
     ```

     Добавьте:

     ```
     your-username ALL=(ALL) NOPASSWD: /usr/bin/mount, /usr/bin/umount, /usr/sbin/mkfs
     ```

     Замените `your-username` на вашего пользователя (например, `w1zard`).

6. **Создайте папку** `templates`:

   - Поместите файл `index.html` (из репозитория) в папку `templates` в корне проекта.

### Запуск приложения

1. Запустите FastAPI-сервер:

   ```bash
   python app.py
   ```
2. Откройте браузер и перейдите на `http://127.0.0.2:8000`.

---

## 📂 Структура проекта

```
disk-manager/
│
├── app.py              # Основной файл FastAPI-приложения
├── templates/
│   └── index.html      # Шаблон интерфейса с Tailwind CSS
├── screenshot.png      # Скриншот интерфейса (добавить вручную)
└── README.md           # Документация
```

---

## 🔍 Использование

1. **Просмотр дисков**:

   - На главной странице (`/`) отображаются все диски и их разделы, полученные с помощью `lsblk`.
   - Для каждого раздела показаны имя, размер, точка монтирования и кнопки управления.

2. **Монтирование**:

   - Введите точку монтирования (например, `/mnt/sda1`) и нажмите "Монтировать".
   - После успешного монтирования страница обновится.

3. **Размонтирование**:

   - Нажмите "Размонтировать" для смонтированного раздела (кнопка отключена, если раздел не смонтирован).

4. **Форматирование**:

   - Выберите файловую систему (`ext4`, `vfat`, `ntfs`) и нажмите "Форматировать".
   - Подтвердите действие в диалоговом окне (форматирование удаляет все данные!).
   - Раздел должен быть размонтирован перед форматированием.

---

## 🛡 Безопасность

- **Изолированная ВМ**: Приложение тестировалось в виртуальной машине (Ubuntu 24.04) с несколькими виртуальными дисками, чтобы избежать риска для основной системы.
- **Защита системных разделов**: Эндпоинт `/format` блокирует форматирование системных разделов (например, `/dev/nvme0n1p7` для `/` и `/dev/nvme0n1p1` для `/boot/efi`).
- **Подтверждение форматирования**: Кнопка "Форматировать" требует подтверждения, чтобы предотвратить случайное удаление данных.

---

## 🔗 Репозиторий

GitHub: Disk Manager

*Примечание*: Замените `your-username` на ваш реальный GitHub-username и загрузите проект в репозиторий.

---

## 🚀 Тестирование

1. **Создайте виртуальную машину** (например, VirtualBox с Ubuntu 24.04).
2. **Добавьте виртуальные диски**:
   - Создайте несколько виртуальных дисков (например, 10 ГБ и 20 ГБ).
   - Подключите USB-устройство (например, флешку) через USB-пасsthrough.
3. **Проверьте диски**:

   ```bash
   lsblk
   ```
4. **Запустите приложение** и протестируйте операции монтирования, размонтирования и форматирования через интерфейс.
5. **Проверьте ошибки**:
   - Если форматирование не работает, убедитесь, что раздел размонтирован и права `sudo` настроены.
   - Используйте эндпоинт `/debug` для отладки:

     ```python
     @app.get("/debug")
     async def debug():
         disks = await get_disks()
         return disks
     ```

     Перейдите на `http://127.0.0.2:8000/debug` для просмотра данных.

---

## 📝 Примечания

- **Автоматическое монтирование**: Ubuntu может автоматически монтировать флешки в `/media/<user>/<UUID>`. Размонтируйте их перед форматированием.
- **Файловые системы**: Поддерживаются `ext4`, `vfat`, и `ntfs`. Для `vfat` и `ntfs` требуются пакеты `dosfstools` и `ntfs-3g`.
- **Скриншот**: Добавьте реальный скриншот интерфейса, заменив `screenshot.png`.